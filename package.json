{
  "name": "nlx-chat-sdk",
  "version": "0.1.0",
  "description": "SDK to talk to NLX bots",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish": "rm packages/*/package-lock.json && lerna publish"
  },
  "author": "Peter Szerzo <peter@nlx.ai>",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}
